import { JoinRoomButton } from "@/features/join-room";

export const JoinRoomFlow = () => {
  return (
    <div>
      <h2>Join Room</h2>
      <JoinRoomButton roomId="demo-room" />
    </div>
  );
};
